import { Plugin } from '@/types/plugin';

const plugin: Plugin = {
  name: 'Auth Plugin',
  path: '/auth',
  version: '1.0.0',
  description: 'Adds authentication features!',
};

export default plugin;
